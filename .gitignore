# dependencies / build output
node_modules/
dist/
src-tauri/target/

# secrets — only .env.example is tracked
.env
.env.*
!.env.example
.gh-tokens.json
# updater signing keys live in ~/.tauri, but guard against a stray copy here
*.key

# WIP / editor artefacts that must never be committed
*.backup
*.orig
*.temp
*.swp
.DS_Store
